# macOS
.DS_Store

# Xcode — user-specific state
xcuserdata/
*.xcuserstate
*.xcuserdatad

# Xcode build / derived data
build/
DerivedData/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# Swift Package Manager
.swiftpm/
.build/

# Claude / agent local state (may contain credentials)
.claude/settings.local.json
